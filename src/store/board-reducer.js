const initialState = {
    boards: [],
}
export function boardReducer(state = initialState, action) {
    var newState = state
    switch (action.type) {
      
        
        default:
    }
    // For debug:
    // window.boardState = newState
    return newState

}
